import { ComingSoon } from '@/components/coming-soon';

export default function RescheduleAppointmentPage() {
  return (
    <ComingSoon
      title="Reschedule Appointment"
      subtitle="Move an existing appointment to a new date or time."
    />
  );
}
