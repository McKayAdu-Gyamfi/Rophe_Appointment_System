# Copy to .env.local
NEXT_PUBLIC_API_URL="http://localhost:4000/api"

# Demo accounts on the login page: on in development, off in production.
# "true" shows them in a deployed build (only if that database is seeded).
# NEXT_PUBLIC_SHOW_DEMO_ACCOUNTS="true"
